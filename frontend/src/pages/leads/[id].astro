---
import PageShell from '../../components/layout/PageShell.astro';
---
<PageShell title="Detalle de Lead">
  <div class="mb-4">
    <a href="/leads" class="text-sm text-gray-500 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400">← Volver a Leads</a>
  </div>
  <div id="lead-detail" class="max-w-2xl">
    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-6 animate-pulse">
      <div class="h-6 bg-gray-100 dark:bg-gray-700 rounded mb-4 w-1/3"></div>
      <div class="h-4 bg-gray-100 dark:bg-gray-700 rounded mb-2"></div>
      <div class="h-4 bg-gray-100 dark:bg-gray-700 rounded w-2/3"></div>
    </div>
  </div>

  <script>
    import { leadsApi, SOURCE_LABELS, STATUS_LABELS, STATUS_COLORS } from '../../lib/api/leads';
    import { initAuth } from '../../lib/auth/store';

    const id = window.location.pathname.split('/').pop()!;
    await initAuth();

    try {
      const lead = await leadsApi.getById(id);
      const container = document.getElementById('lead-detail')!;

      container.innerHTML = `
        <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 shadow-sm p-6 space-y-4">
          <div class="flex items-start justify-between">
            <div>
              <span class="text-xs px-2 py-1 rounded-full font-medium ${STATUS_COLORS[lead.status]}">
                ${STATUS_LABELS[lead.status]}
              </span>
            </div>
            <span class="text-xs text-gray-400 dark:text-gray-500">${new Date(lead.created_at).toLocaleDateString('es-MX')}</span>
          </div>

          <div class="grid grid-cols-2 gap-4 text-sm">
            <div>
              <p class="text-xs text-gray-500 dark:text-gray-400">Fuente</p>
              <p class="font-medium text-gray-900 dark:text-gray-100">${SOURCE_LABELS[lead.source]}</p>
            </div>
            <div>
              <p class="text-xs text-gray-500 dark:text-gray-400">Vehiculo de interes</p>
              <p class="font-medium text-gray-900 dark:text-gray-100">${[lead.interest_year, lead.interest_make, lead.interest_model].filter(Boolean).join(' ') || '—'}</p>
            </div>
            <div>
              <p class="text-xs text-gray-500 dark:text-gray-400">Presupuesto minimo</p>
              <p class="font-medium text-gray-900 dark:text-gray-100">${lead.budget_min ? '$' + lead.budget_min : '—'}</p>
            </div>
            <div>
              <p class="text-xs text-gray-500 dark:text-gray-400">Presupuesto maximo</p>
              <p class="font-medium text-gray-900 dark:text-gray-100">${lead.budget_max ? '$' + lead.budget_max : '—'}</p>
            </div>
          </div>

          ${lead.notes ? `
          <div>
            <p class="text-xs text-gray-500 dark:text-gray-400 mb-1">Notas</p>
            <p class="text-sm text-gray-700 dark:text-gray-300">${lead.notes}</p>
          </div>` : ''}
        </div>
      `;
    } catch {
      document.getElementById('lead-detail')!.innerHTML =
        '<p class="text-red-500 dark:text-red-400 text-sm">Error cargando el lead</p>';
    }
  </script>
</PageShell>
